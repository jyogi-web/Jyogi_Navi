[project]
name = "notion-ingest"
version = "0.1.0"
description = "Notion ページ取得・正規化・Dify アップロードスクリプト"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
